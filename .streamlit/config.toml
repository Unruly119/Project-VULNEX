# Force light theme so Fable's warm-parchment palette renders consistently
# regardless of the user's OS / browser dark-mode preference.
[theme]
base = "light"
primaryColor = "#c4622d"
backgroundColor = "#f5f0e8"
secondaryBackgroundColor = "#faf8f4"
textColor = "#141413"
font = "sans serif"
