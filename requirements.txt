streamlit>=1.35.0
httpx[http2]>=0.27.0
h2>=4.1.0
beautifulsoup4>=4.12.0
google-generativeai>=0.7.0
cachetools>=5.3.0
lxml>=5.0.0
cryptography>=42.0.0
python-dotenv>=1.0.0
dnspython>=2.6.0
# HTML→PDF report engine (run once after install:  python -m playwright install chromium)
# NOTE: replaced ReportLab — the report is rendered as HTML by headless Chromium.
playwright>=1.40.0
# ── Membership / backend ──────────────────────────────────────────
# Login/signup + scan-history/audit are stored in Supabase (PostgREST over the
# existing httpx dep — no supabase-py needed). This adds the browser-side login
# cookie component used by src/auth.py.
streamlit-cookies-controller>=0.0.4
